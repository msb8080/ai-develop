# Rainbow AI Dev Copilot 协作规则

本文件是本工作空间的唯一维护规则源。任何人或 AI Agent 修改本项目时都必须遵守。

## 开始工作前

1. 阅读 `README.md`、`docs/PROGRESS.md` 和当前阶段文档。
2. 确认本次任务对应的里程碑、验收条件与边界。
3. 保持 Java 为主业务链路；Python 用于评测和实验，TypeScript 用于前端。
4. 未经明确授权，不执行生产部署、外部发布、付费调用或破坏性操作。

## 完成定义

一次改动只有同时满足以下条件才算完成：

- 实现或文档变更已经验证。
- 相关测试、构建或人工检查已执行。
- 受影响的维护文档已同步。
- `docs/PROGRESS.md` 已记录结果、证据和下一步。
- `README.md` 中的当前阶段与实际状态一致。

禁止把文档同步留作“以后处理”。

## 文档影响矩阵

| 改动类型 | 必须检查并同步 |
|---|---|
| 完成任务、阶段进展、阻塞变化 | `docs/PROGRESS.md`、对应阶段文档 |
| 产品目标、能力或范围变化 | `docs/MASTER_PLAN.md`、`README.md` |
| 架构、模块、数据流或部署变化 | `docs/ARCHITECTURE.md` |
| 框架、语言、模型或基础设施选型 | `docs/TECHNOLOGY_MATRIX.md`、`docs/decisions/` |
| 排期、优先级或里程碑变化 | `docs/ROADMAP.md` |
| 用户可见行为变化 | `docs/CHANGELOG.md` |
| API、Agent 事件或协议变化 | `docs/ARCHITECTURE.md`、对应阶段文档 |

如果无需修改文档，交付说明必须包含“文档影响：无”及原因。

## 进度记录规则

- 状态只使用：`未开始`、`进行中`、`阻塞`、`已完成`、`已取消`。
- 完成比例按“已完成验收项 / 验收项总数”计算。
- “已完成”必须附带代码、测试结果、截图、部署验证或文档链接之一。
- 每个阶段结束时记录计划耗时、实际耗时、偏差原因和面试可讲成果。
- 日期使用 `YYYY-MM-DD`，文档内容以中文为主，技术标识保留英文。

## Git 规则

- 主分支名称为 `main`。
- 提交信息使用 `<type>: <subject>`，其中 `type` 取 `feat`、`fix`、`docs`、`test`、`refactor`、`chore`。
- 每个提交只表达一个完整意图，代码、测试和对应文档同步进入同一提交。
- 未经用户明确要求，不创建远程仓库、不推送、不强制改写历史。
- 提交前检查 `git diff`、测试结果和文档影响，不提交密钥、本地数据或构建产物。

## 安全边界

- 云端只执行只读演示任务，不运行不可信代码。
- 本地命令执行进入临时沙盒；写文件、联网、部署等操作需要人工确认。
- 不读取、记录或转发 Codex、Claude Code、OpenCode 的登录凭据。
- 不把 Docker Socket、SSH 目录、云凭据或用户主目录挂载进执行沙盒。
