# CodeLens AI 迁移说明

## 2026-09-06 当前决策与实施（优先于下方历史计划）

- 用户明确要求：整合到 ai-develop、优化重构、更新个人主页、下线 codelens-ai，并保持非公开。
- ai-develop 继续为私有仓库；源码、本地离线演示和真实模型接口均不公开。
- 实施方式是能力重构，不执行带有历史凭据的 unrelated-history 合并。原仓库及分支历史保留，不删除、不归档。
- 来源：codelens-ai main `353ceff5d1940a592c46af7ebf588b681b5277e6`；开发分支 `2a89c70a7795fbf8b457ba131c6353c110601bf6`。检查了两分支差异，后者主要为较早 UI/服务版本。

| 原能力 | 新实现或收敛决策 | 验收 |
| --- | --- | --- |
| 9 个角色 | 服务端注册 general/chef/stock/gamer/prd/design/code/test/deploy，加上原有审查和诊断共 11 个 Skill | 全部资源加载测试通过 |
| Slash 命令 | /explain /optimize /bug /test /refactor /doc /review 在服务端路由，显式 Skill 优先 | 测试验证命令优先于关键词 |
| 流式对话/停止 | 复用 Spring AI 网关和持久化会话；前端 AbortController、读流释放、尾帧及异常断流提示 | 后端测试与本地离线交互验证 |
| 配置导入/导出 | 仅导入角色偏好（skillId/activeAgentId），导出白名单字段；限制 100KB | 构建通过；不迁入地址、凭据或任意命令 |
| 自定义 Agents/Skills/Rules | 改为源码管理的服务端 Skill 资源及 ContextAssembler 系统约束 | 不再允许浏览器修改系统提示；旧自定义配置需人工转成服务端资源 |
| 多模型 | 复用服务端模型配置与提供商网关 | 不再提供浏览器端 API Key 或任意地址输入 |
| MCP | 复用受控只读 MCP 和独立审批沙盒 | 不导入旧配置内任意 stdio 命令 |
| 对话导出 | Markdown 文件下载 | 前端类型检查通过 |
| RAG | 按既定范围删除，不迁移占位实现 | 保持无向量数据库依赖 |
| 反馈/Markdown 预览/拖拽附件 | 本批不复制旧前端整套编辑器；原实现保留于源仓库 | 不宣称一比一功能迁移 |

验证：指定 Maven settings + Java 17，13 项测试通过（0 失败、0 跳过）；前端 `npm test` 3 项通过（Node 24，取消保留部分历史、分片中文/CRLF/尾帧、异常断流）；普通/离线模式 Vite 构建通过；本地浏览器验证角色、演示响应及停止后的 cancelled 状态。未调用付费模型。

下线进度（2026-09-06）：GitHub Pages 已停用，删除托管配置后 GET pages 返回 404、仓库 has_pages=false；可重新启用托管恢复，不影响代码历史。个人主页 5f007f2 已部署成功，线上 DOM 确认无旧站链接。ai-develop dbe7c89 已推送并再次确认 private=true。

Render 后端管理页需要登录，尚未停用。连接旧地址超时不等于已下线。旧 codelens-ai 仓库仍为 public、未归档；已请求用户确认是否也转私有。历史密钥轮换仍需提供商控制台确认。

## 2026-09-04 历史计划

最后更新：2026-09-04
状态：进行中；源仓库保留，不删除、不归档

## 结论

`ai-develop` 不是 `codelens-ai` 的完整副本。两者在 AI 对话与 Agent 概念上有重叠，但代码、技术栈和完成度不同。迁移采用“提炼能力、重新实现、逐项验收”的方式，不直接合并 Git 目录，也不以存在相似代码作为删除源仓库的依据。

## 仓库定位

| 能力 | `codelens-ai` | `ai-develop` 迁移目标 | 当前状态 |
|---|---|---|---|
| 流式对话 | 已有 SSE 与模型调用 | 服务端控制的稳定 SSE 协议 | 协议与测试完成，真实模型待接入 |
| 前端工作台 | 静态 HTML/CSS/JavaScript | React + TypeScript 工作台 | 未迁移 |
| Agent/Skills/Rules | 前端内置多套配置 | Java 编排与可审计配置 | 未迁移 |
| 项目上下文 | 主要由前端配置和用户输入提供 | 服务端按需组装项目文件、规则、工具结果和历史 | 已纳入规划 |
| Git 历史 | 54 个提交及独立开发分支 | 保留来源与迁移证据 | 源仓库继续保留 |

## 安全约束

- 新接口只接收用户消息，不接收客户端提供的 `baseUrl`、API Key、模型名称或系统提示词。
- 提供商凭据只存在于服务端环境配置中，不记录到日志、指标标签、异常消息或数据库明文。
- 不复制源仓库历史 `.env` 或任何真实凭据。
- 源仓库历史提交曾暴露 Xiaomi MiMo API Key。必须在提供商控制台撤销或轮换；重写 Git 历史、删除文件或归档仓库都不能使已泄露密钥自动失效。
- 用户鉴权、会话归属和多租户隔离完成前，对话接口仅用于本地开发验证。

## 分阶段执行

1. **安全协议骨架（已完成）**：建立 `ChatGateway`、单消息请求、结构化 SSE 事件、超时取消、指标和自动化测试。
2. **真实模型接入（待确认）**：选择模型提供商，通过服务端配置实现网关，并补充超时、限流、费用与内容安全测试。
3. **上下文与 Skills（未开始）**：按需读取项目文件、压缩会话、限制上下文预算，并把旧项目 Agent 配置转为可发现、可版本化的 Skills。
4. **前端能力重建（未开始）**：盘点旧前端交互，在 React + TypeScript 中按需重建；不原样搬运客户端密钥配置。
5. **迁移验收（未开始）**：功能、测试、安全、历史和文档全部通过后，由维护者人工决定是否归档源仓库。

## 源仓库归档门槛

- 迁移清单逐项确认，旧仓库仍有而新仓库缺失的能力已明确保留或放弃。
- 新仓库全量测试、构建和最小运行验收通过。
- 源仓库 54 个提交与分支历史仍可恢复，并记录新旧仓库关系。
- 历史泄露密钥已在提供商侧撤销或轮换。
- [x] `ai-develop` 已使用 noreply 身份建立私有 GitHub 仓库并同步 `main` 基线。
- 维护者再次明确批准归档；未满足时保持两个仓库并存。

## 风险与待办

- 模型提供商、预算和网络条件尚未确定，真实回答能力仍被阻塞。
- 当前 SSE 是单轮无状态实现，不能宣称已迁移旧项目的会话体验。
- 旧前端包含较多产品交互，重建前需要做页面和配置能力清单，避免遗漏。
- 新仓库当前保持私有；改为公开前必须再次执行敏感信息、历史和文档链接检查。
