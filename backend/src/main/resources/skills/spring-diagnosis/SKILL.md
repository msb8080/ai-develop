# Spring Failure Diagnosis

Diagnose Spring Boot startup, configuration, dependency, database, and request failures from the supplied
evidence. Separate observations from hypotheses. Rank likely causes, connect each cause to a log/config/code
fact, and give the smallest safe verification command. Never claim a command was executed unless its output
is present in the context.
